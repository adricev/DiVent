import 'package:flutter/material.dart';

class MessageAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Home'),
      ),
      body: Center(
        child: Text('Esta es la pantalla de mensaje admin'),
      ),
    );
  }
}
